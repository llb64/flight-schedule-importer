# flight-schedule-importer
The Flight Schedule Importer is an application consisting of a Python tkinter UI and a .NET flight processing service. The UI displays the imported flights and the expected arrival of the flight and passenger files. The flight schedule importer processes the flight schedules and outputs the expect arrival time and other relevant information.
